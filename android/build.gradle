group 'tech.mastersam.livechat'
version '1.0'

buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:4.1.2'
    }
}

rootProject.allprojects {
    repositories {
        google()
        jcenter()
        maven {
            url "https://api.bitbucket.org/2.0/repositories/clanq/chat-window-android-fork/src/master"
            credentials {
                username "DjordjeMancic"
                password "aaf09073"
            }
            authentication{
                basic(BasicAuthentication)
            }
        }
    }
}

apply plugin: 'com.android.library'

android {
    compileSdkVersion 30

    defaultConfig {
        minSdkVersion 16
    }
    lintOptions {
        disable 'InvalidPackage'
    }
}

dependencies {
    implementation(group: 'de.clanq.chat', name: 'chat-window', version: '1.0.0', ext: 'aar')
}